/** Aqui van a ir los colores que se utilizaran en toda la aplicacion, esto
 * para que cuando se haga el darkMode se cambie solo en un lado.
 * */
const Palette = {
  mainColor: "#7E42FF",
  secondaryColor: "#E336FF",
  blueColor: "#3662FF",
};

export default Palette;
